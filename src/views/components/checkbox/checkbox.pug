include /src/views/libs/bemto/bemto

mixin checkbox(label)
    +b.checkbox-field
        +e.LABEL.label
            input(type="checkbox")
            +e.SPAN.input
            +e.SPAN.title.body=label
            block